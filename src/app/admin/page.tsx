import AdminClient from "./AdminClient";

export const metadata = {
  title: "Admin | Camille & Scott's Baby Pool",
  robots: "noindex, nofollow",
};

export default function AdminPage() {
  return <AdminClient />;
}
